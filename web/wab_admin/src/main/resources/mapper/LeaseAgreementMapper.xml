<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nanhang.lease.web.admin.mapper.LeaseAgreementMapper">

    <select id="selectByPage" resultType="com.nanhang.lease.web.admin.vo.agreement.AgreementVo">


        select
            la.id,
            la.phone,
            la.name,
            la.identification_number,
            la.apartment_id,
            la.room_id,
            la.lease_start_date,
            la.lease_end_date,
            la.lease_term_id,
            la.rent,
            la.deposit,
            la.payment_type_id,
            la.status,
            la.source_type,
            la.additional_info,


            ai.id apartmentId,
            ai.name apartmentName,
            ai.introduction,
            ai.district_id,
            ai.district_name,
            ai.city_id,
            ai.city_name,
            ai.province_id,
            ai.province_name,
            ai.address_detail,
            ai.latitude,
            ai.longitude,
            ai.phone apartmentPhone,
            ai.is_release,
            ri.id roomId,
            ri.room_number,
            ri.rent roomRent,
            ri.apartment_id,
            ri.is_release,
            pt.id paymentTypeId,
            pt.name paymentTypeName,
            pt.pay_month_count,
            pt.additional_info,
            lt.id leaseTermId,
            lt.month_count,
            lt.unit


            from
                lease_agreement la
            left join
                apartment_info ai
                    on la.apartment_id = ai.id


        left join
                room_info ri
                    on la.room_id = ri.id

        left join
                payment_type pt
                    on la.payment_type_id = pt.id
        left join
                lease_term lt
                    on la.lease_term_id = lt.id


            <where>
                la.is_deleted = 0
            <if test="queryVo.provinceId != null">
                and ai.province_id = #{queryVo.provinceId}
            </if>
            <if test="queryVo.cityId != null">
                and ai.city_id = #{queryVo.cityId}
            </if>
            <if test="queryVo.districtId != null">
                and ai.district_id = #{queryVo.districtId}
            </if>
            <if test="queryVo.apartmentId != null">
                and la.apartment_id = #{queryVo.apartmentId}
            </if>
            <if test="queryVo.roomNumber != null">
                and ri.room_number = #{queryVo.roomNumber}
            </if>
            <if test = "queryVo.name != null">
                and la.name like concat('%',#{queryVo.name},'%')
            </if>
            <if test="queryVo.phone != null">
                and la.phone = #{queryVo.phone}
            </if>


            </where>



    </select>
</mapper>
